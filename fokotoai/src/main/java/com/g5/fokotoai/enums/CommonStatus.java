package com.g5.fokotoai.enums;

/**
 * Dùng chung cho các bảng có ENUM('ACTIVE','INACTIVE'):
 * vocabulary.status, articles.status, exam_categories.status,
 * questions.status, exam_templates.status, subscription_packages.status
 */
public enum CommonStatus {
    ACTIVE,
    INACTIVE
}
