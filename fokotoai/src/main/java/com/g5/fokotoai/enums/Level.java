package com.g5.fokotoai.enums;

/**
 * JLPT Level — dùng chung cho nhiều bảng:
 * vocabulary.level, vocabulary_chapters.level, articles.level,
 * exam_categories.level, questions.level, exam_templates.level,
 * rank_matches.level, quiz_attempts (qua template)
 */
public enum Level {
    N5,
    N4
}
